<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.potato.mapper.Manner_mapper">

<!-- 회원의 매너칭찬 리스트 조회 -->
<select id="findAllByMember" parameterType="java.lang.String" resultType="com.potato.domain.MannerVO">
    <![CDATA[
    SELECT * FROM manner
    WHERE member_number = #{member_number}
    ORDER BY mcount DESC
    ]]>
</select>

<!-- 칭찬하기 데이터 전송 -->
<update id="incrementMannerCount">
	<![CDATA[
    UPDATE manner
    SET mcount = mcount + 1
    WHERE manner_number = #{manner_number}
    AND member_number = #{member_number}
    ]]>
</update>

</mapper>